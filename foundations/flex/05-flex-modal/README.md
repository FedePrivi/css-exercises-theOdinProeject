# A common 'modal' style
This one is another very common pattern on the web. The solution to this one is _simple_... but it might not be immediately obvious to you. You'll need to edit the HTML a bit to get everything where it needs to be.

### A hint
Depending on how you approach this one, you might need to revisit the `flex-shrink` property to keep a flex item from getting smashed. In addition, pay attention to the structure of the html, specifically look into adding an additional container surrounding the header, button, main text, cancel, and continue divs; and look into moving the header div to encompass the button as well.

## Desired outcome

![desired outcome](./desired-outcome.png)

### Self Check

- The blue icon is aligned to the left.
- There is equal space on either side of the icon (the gaps between the icon and the edge of the card, and the icon and the text, are the same).
- There is padding around the edge of the modal.
- The header, text, and buttons are aligned with each other.
- The header is bold and a slightly larger text-size than the text.
- The close button is vertically aligned with the header, and aligned in the top-right of the card.

----------------------------------------------------------------------------------


Un estilo “modal” común

Este es otro patrón muy común en la web. La solución para este es simple… pero puede que no sea inmediatamente obvia. Necesitarás editar un poco el HTML para colocar todo donde corresponde.

Una pista

Dependiendo de cómo enfoques este ejercicio, quizá debas revisar la propiedad flex-shrink para evitar que un elemento flex se comprima demasiado. Además, presta atención a la estructura del HTML: específicamente, considera agregar un contenedor adicional que envuelva el encabezado, el botón, el texto principal y los divs de “cancelar” y “continuar”; y piensa en mover el div del encabezado para que también incluya el botón.

Resultado deseado

Autoevaluación:

-El ícono azul está alineado a la izquierda.

-Hay un espacio igual a ambos lados del ícono (los márgenes entre el ícono y el borde de la tarjeta, y entre el ícono y el texto, son los mismos).

-Hay padding alrededor del borde del modal.

-El encabezado, el texto y los botones están alineados entre sí.

-El encabezado está en negrita y tiene un tamaño ligeramente mayor que el texto.

-El botón de cerrar está alineado verticalmente con el encabezado y ubicado en la esquina superior derecha de la tarjeta.